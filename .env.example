# Database
DATABASE_URL=postgresql://tac:password@localhost:5432/tacbookings
DB_PASSWORD=password                   # Used by docker-compose for PostgreSQL
SHADOW_DATABASE_URL=                   # Optional. Used by `npm run db:check-drift` (and Prisma migrate dev) to replay migrations into a throwaway DB; leave empty outside CI/local drift checks

# NextAuth
NEXTAUTH_URL=http://localhost:3000
# Sign-in, 2FA, AND encryption of in-app provider credentials all derive from this
# secret. Use a strong value (>= 32 chars). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Rotating it drops all sessions, all 2FA enrolments/recovery codes, and all
# stored provider credentials — see DEPLOYMENT.md (auth-secret rotation runbook).
AUTH_SECRET=your-secret-key-here-change-in-production
NEXTAUTH_SECRET=your-secret-key-here-change-in-production  # Legacy fallback; keep aligned with AUTH_SECRET
AUTH_TRUST_HOST=true

# Is this installation the club's LIVE site, or a copy of it? REQUIRED.
# Exactly one of: production | non-production
#
# Nothing in this app guesses. Not NODE_ENV (a build mode - a staging container
# runs a production build), not the hostname, not the database it is pointed at
# (a copy restored from the live database looks identical), and not
# APP_RUNTIME_ROLE below, which names the container slot. Every one of those is a
# convention that holds until somebody stands up a copy that breaks it, and that
# is exactly the day it matters.
#
# If it is unset or holds anything else, the app reports "not configured" rather
# than picking one - and from that state it holds back anything whose safety
# depends on knowing which installation this is, because a copy of the live
# database holds real members and their real email addresses.
#
# THIS TEMPLATE SHIPS THE SAFE VALUE, because it is a local-development template
# (localhost URLs, seed passwords). The club's live deployment MUST set
# `production` in its own .env; scripts/run-production-blue-green-deploy.sh
# refuses to deploy without it, at step 3 of 20, before the migration runs and
# long before any traffic moves. See docs/guides/environment-role.md.
APP_ENVIRONMENT_ROLE=non-production

# Operational defaults
CURRENCY=NZD
NEXT_PUBLIC_CURRENCY=NZD
# A SEED ONLY since #2989. The club's time zone is recorded in the database
# and edited at Admin -> Setup & Configuration -> Club Time Zone; these two are what a fresh
# install, or the first boot after upgrading, copies its zone FROM. After that
# the recorded setting is the authority and changing these does not change what
# members see. Not the same thing as the container's own clock, which is
# deliberately irrelevant to club time. See docs/guides/club-time.md.
TZ=Pacific/Auckland
NEXT_PUBLIC_TZ=Pacific/Auckland
LOCALE=en-NZ
NEXT_PUBLIC_LOCALE=en-NZ

# Optional modules are turned on or off entirely from the admin Modules page
# (Admin > Setup > Modules). There are no module feature-flag env vars.
# Google Analytics is NO LONGER environment configuration (#2573). Turn the module
# on at Admin > Modules, then enter the GA4 measurement id and choose the visitor
# consent settings at Admin > Integrations > Google Analytics. Any legacy
# NEXT_PUBLIC_GA_MEASUREMENT_ID value is ignored: nothing in the app reads it, so
# analytics stays off until the in-app setup is completed. Remove it from your env.
# Google sign-in credentials are NO LONGER environment configuration (#2087).
# Enter and verify the per-club Google OAuth client id + secret in-app at
# Admin > Integrations > Google sign-in (encrypted at rest). Any GOOGLE_CLIENT_*
# env vars are ignored (detected + warned about) — re-enter in the wizard, then
# remove them.

# Local/staging seed admin. Required by `npm run db:seed`; choose your own values.
SEED_ADMIN_EMAIL=admin@example.org
SEED_ADMIN_PASSWORD=change-this-seed-password
# Optional display name for the seeded admin (defaults to "Admin User")
SEED_ADMIN_FIRST_NAME=
SEED_ADMIN_LAST_NAME=
SEED_LODGE_PASSWORD=change-this-lodge-kiosk-password

# Stripe
# Stripe CREDENTIALS (secret key, publishable key, webhook signing secret) are
# captured IN-APP now and stored encrypted in the database (Admin > Integrations
# > Stripe) — #2082. There are no STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET /
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY env vars any more; if any are still set they
# are IGNORED and flagged in setup readiness for removal (see CONFIGURATION.md and
# the DEPLOYMENT.md upgrade runbook). The publishable key is delivered to the
# browser at runtime from the store, so nothing Stripe is inlined at build time.

# Xero
# Xero CREDENTIALS (client id/secret, webhook key, the token-encryption key and
# the redirect URI) are captured IN-APP now and stored encrypted in the database
# (Admin > Integrations). There are no XERO_CLIENT_ID / XERO_CLIENT_SECRET /
# XERO_REDIRECT_URI / XERO_ENCRYPTION_KEY / XERO_WEBHOOK_KEY env vars any more —
# if any are still set they are IGNORED and flagged in setup readiness for
# removal (see CONFIGURATION.md and DEPLOYMENT.md upgrade runbook). The redirect
# URI derives from NEXTAUTH_URL. Only the operational tuning flags below remain:
XERO_ENABLE_DAILY_MEMBERSHIP_REFRESH=false
XERO_ENABLE_LIVE_MEMBER_GROUP_LOOKUPS=false
XERO_ENABLE_AUTOLOAD_XERO_CONTACT_GROUPS=false
# The finance dashboard reads Xero reports through this same operational connection.
# Reconnect Xero after deploying so the accounting.reports.read scope is granted.

# Email (AWS SES)
USE_AWS_SES=false        # exactly ONE of USE_AWS_SES / USE_SMTP_RELAY / USE_LOCAL_CAPTURE may be true
SMTP_HOST=email-smtp.ap-southeast-2.amazonaws.com
SMTP_PORT=587
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=
SES_SNS_TOPIC_ARN=     # Required for SES bounce/complaint SNS webhook topic allowlisting
SES_SNS_ALLOW_UNSAFE_MISSING_TOPIC_ARN=false  # Local/dev only; never enable for deployed SES feedback ingestion
SES_SNS_ALLOW_SIGNATURE_V1=false  # Temporarily allow legacy SNS SignatureVersion 1 (SHA1); prefer SignatureVersion 2 on the topic

# Transactional Email System (external SMTP relay)
USE_SMTP_RELAY=false        # exactly ONE provider flag may be true
EMAIL_SERVER_HOST=smtp.example.org
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=smtp-user@example.org
EMAIL_SERVER_PASSWORD=

# Local capture mailbox (ENV-SAFETY 2, #3035). LEAVE THIS false ON THE CLUB'S
# LIVE SITE — an installation that declares APP_ENVIRONMENT_ROLE=production and
# USE_LOCAL_CAPTURE=true at the same time is REFUSED, because a live site in
# capture mode would accept every message and deliver none of them.
#
# It is for a copy: a staging or test installation
# (APP_ENVIRONMENT_ROLE=non-production) normally suppresses every send rather
# than contact a provider, which is what stops a copy emailing the club's real
# members. Declaring a capture mailbox says "this transport is a sink that cannot
# deliver onward", and such a copy is then allowed to transmit into it — which is
# how the browser test suite reads its own mail back out of mailpit. It uses the
# same four EMAIL_SERVER_* settings as the relay above; only the declaration
# differs, and nothing is ever inferred from the host name.
#
# ON A DEVELOPER'S LAPTOP THIS IS ALSO WHAT MAKES EMAIL DO ANYTHING AT ALL, and
# it is worth knowing before you go looking for a bug that is not there. With the
# values shipped here — this file declares APP_ENVIRONMENT_ROLE=non-production and
# all three provider flags false — every send is HELD BACK at the delivery
# boundary, so the familiar "Email sent (dev mode)" log line never appears and
# `npm run dev` behaves as a copy should. To get local mail flowing again, run a
# capture mailbox (mailpit: `docker run -p 1025:1025 -p 8025:8025
# axllent/mailpit`), point the four EMAIL_SERVER_* settings above at it, and set
# USE_LOCAL_CAPTURE=true. The dev-mode short-circuit then runs as before.
USE_LOCAL_CAPTURE=false     # exactly ONE provider flag may be true

# Allows the capture declaration above when EMAIL_SERVER_HOST is a host on the
# public internet (#3071 review). Leave it false unless you know you need it.
#
# WHAT IT IS FOR. Declaring USE_LOCAL_CAPTURE=true while EMAIL_SERVER_HOST still
# names a real relay is REFUSED, and mail is held back rather than sent. That
# refusal exists because it was the shape of a real defect: an installation that
# already had USE_SMTP_RELAY working, told to "set USE_LOCAL_CAPTURE=true",
# flipped the one flag and kept its live relay host — so a copy emailed real
# members while every log line said the message had reached nobody. A container
# name (mailpit), localhost, any private address and the reserved suffixes
# (.local, .internal, .home.arpa, .test, .invalid, .example) are all accepted
# without this flag.
#
# Set it to true ONLY if your capture mailbox genuinely forwards nothing but
# happens to have a public name. Nothing checks that, and nothing can: a mail
# server on a private address can relay outward too. This flag is you taking
# responsibility for the claim.
EMAIL_CAPTURE_ALLOW_PUBLIC_HOST=false

# Extra email Variables
# Email identity (from display name, support address, contact-form recipient) is
# admin-managed in Admin > Email Messages (DB-first EmailMessageSetting). EMAIL_FROM
# is the ONLY email env var besides transport secrets: it is the envelope /
# Return-Path sender and MUST be a provider-verified (SES) address in production.
EMAIL_FROM=bookings@example.org
BULK_SENDMAIL_LIMIT=1                  # Admin bulk communication sends allowed per hour

# Deployment
DOMAIN=example.org                    # Root public host used by Caddy for auto HTTPS and service subdomains
CRON_LEADER_RUNTIME_STATUS_URL=       # Optional internal URL for Admin > System Health to query the cron leader; Compose defaults to http://app:3000/api/deploy/runtime-status

# Addy address autocomplete (server-side proxy)
ADDY_API_KEY=
ADDY_API_SECRET=

# MiroTalk video meetings (calendar events). Base URL of your self-hosted
# MiroTalk instance; the join link is built server-side as <MIROTALK_URL>/join/<room>.
# Runtime setting (set + restart, no rebuild). Include the scheme; a bare host is
# assumed https. When unset it is DERIVED from the app's own domain (NEXTAUTH_URL)
# as https://meet.<domain> — so a prod deploy that forgets this points at a real
# same-domain host, not the clicker's machine; a loopback app host falls back to
# http://localhost:3010 for local dev. Set it explicitly if MiroTalk is on a
# different host, or if the app runs on a non-www subdomain.
MIROTALK_URL=                         # e.g. https://meet.example.org

# MiroTalk JWT access (optional — secure, login-free join links). Set all three
# to have the app append a short-lived signed ?token= to meeting links. Secrets;
# server-side only, never sent to the browser.
#  - MIRO_JWT_KEY must equal MiroTalk's own JWT_KEY, and must be a GENERATED
#    secret of at least 32 characters: `openssl rand -base64 32`. Set that same
#    value on both sides. It signs every join token AND encrypts the host
#    credentials inside it, so anyone who guesses it can mint a token MiroTalk
#    accepts as host. Do NOT keep the value MiroTalk ships in its own
#    .env.template — it is published. A weak key logs one server-side warning
#    the first time a link is issued, and still works.
#  - MIRO_MEETING_USERNAME/PASSWORD must match one entry in MiroTalk's HOST_USERS.
#  - MIRO_MEETING_PRESENTER=true (default) makes the clicker the host so the
#    meeting starts immediately; false leaves joiners on MiroTalk's
#    "waiting for host" screen.
#  - MIRO_JWT_EXP is the token lifetime (e.g. 1h, 30m, 900). Default 1h.
MIRO_JWT_KEY=
MIRO_JWT_EXP=1h
MIRO_MEETING_USERNAME=
MIRO_MEETING_PASSWORD=
MIRO_MEETING_PRESENTER=true

# MiroTalk reverse proxy (Caddy `meet` block). MEET_HOST is the EXACT public
# hostname you browse to for meetings — it is NOT auto-derived from DOMAIN, so
# set it explicitly (e.g. meet.lwtc.org.nz) and point its DNS A record at this VM.
# MIROTALK_UPSTREAM is where Caddy reaches MiroTalk: host.docker.internal:3010
# for a MiroTalk started with its own `docker run` (default), or a compose
# service name like mirotalk:3010. Must match MIROTALK_URL's host above.
MEET_HOST=                            # e.g. meet.lwtc.org.nz
MIROTALK_UPSTREAM=host.docker.internal:3010

# Sentry
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=                          # Sentry organization slug (for source map uploads)
SENTRY_PROJECT=                      # Sentry project slug (for source map uploads)
SENTRY_AUTH_TOKEN=                   # Sentry auth token (for source map uploads during build)

# Logging
LOG_LEVEL=info                       # debug, info, warn, error, fatal

# Cron
CRON_SECRET=your-cron-secret-here

# Legacy finance dashboard bridge
LEGACY_DASHBOARD_EXPORT_TOKEN=       # Optional shared bearer token; leave empty to disable the legacy export bridge

# Build-time Node flags (optional, docker compose build only)
# Passed to `next build` as NODE_OPTIONS inside the builder stage, and to nothing
# else — not the running container, not your shell. Leave it empty unless the
# build is being OOM-killed on a small server, which looks like the build dying
# with no error or "JavaScript heap out of memory":
NODE_BUILD_OPTIONS=                  # e.g. --max-old-space-size=4096
#
# Better still, if your server is tight on memory: do not build there at all.
# Set APP_IMAGE to an image CI has already published and `docker compose up -d`
# pulls it instead of compiling (see DEPLOYMENT.md).

# Database Backups
# Backups are configured IN-APP (Admin -> Integrations -> Database Backups,
# /admin/backups) and stored encrypted (#2095). The enabled switch, S3
# bucket/region, access key/secret, retention, and restore-validation URL are NO
# LONGER read from the environment — do not set BACKUP_ENABLED / BACKUP_S3_* /
# BACKUP_RETENTION_DAYS / BACKUP_RESTORE_VALIDATION_URL here (they trigger a
# "no longer used" warning on the Backups page). Only the nightly schedule stays
# in the environment:
BACKUP_CRON_SCHEDULE=0 3 * * *       # Cron schedule for the nightly backup (default: daily 3 AM)

# Local (on-host) backup destination. TWO variables, because a container and its
# host do not share a filesystem and confusing the two is the mistake this pair
# exists to prevent:
#
#   BACKUP_LOCAL_HOST_DIR  the directory ON THE HOST that backup files land in.
#                          Used ONLY by docker-compose, to bind-mount that
#                          directory into the app container. Leave it EMPTY and
#                          the stack uses a named volume instead (backup_data) —
#                          fine unless you want to copy the files off the box.
#                          A host directory must be owned by uid 1001, the user
#                          the app runs as:  sudo chown -R 1001:1001 <dir>
#
#   BACKUP_LOCAL_DIR       the path INSIDE the container that the same mount
#                          lands on. This is what the app writes to and what
#                          /admin/backups pre-fills as the backup directory, so
#                          an operator never has to retype it. Change it only if
#                          /backups clashes with something.
#
# Enter the CONTAINER path on /admin/backups, never the host path. A stored
# value set there overrides BACKUP_LOCAL_DIR.
BACKUP_LOCAL_HOST_DIR=               # e.g. /home/lwtc_lodge/db_backup (empty = named volume)
BACKUP_LOCAL_DIR=/backups            # Container path the mount lands on

# Message board images (epic #2992)
# Mounted exactly like the backup directory above, and for a related reason:
# these files must NOT sit under the application directory, because anything
# there is served by the web server without a session check. Board images are
# member-generated content on a board only signed-in members can read, so they
# live on their own mount and are served by a route that checks the session.
#
#   POST_IMAGE_HOST_DIR   the HOST directory bound to the mount. Leave empty and
#                         the stack uses a named volume instead (post_image_data).
#                         A host directory must be owned by uid 1001, the user
#                         the app runs as:  sudo chown -R 1001:1001 <dir>
#
#   POST_IMAGE_DIR        the path INSIDE the container the same mount lands on.
#                         This is what the app reads and writes. Change it only
#                         if /images clashes with something.
#
# NOT the same thing as the Image Manager's uploads, which are public by design
# and stay under /app/public/images on the image_uploads volume.
POST_IMAGE_HOST_DIR=                 # e.g. /home/lwtc_lodge/post_images (empty = named volume)
POST_IMAGE_DIR=/images               # Container path the mount lands on

# Audit retention archive (optional separate PostgreSQL database)
AUDIT_ARCHIVE_DATABASE_URL=          # Preferred archive DB URL for AuditLogArchive rows
AUDIT_LOG_ARCHIVE_DATABASE_URL=      # Backward-compatible alias; leave empty when preferred var is set

# AI Diagnostics — dedicated SELECT-only database role (required to enable the
# AI Diagnostics module; ADR-007). This is a SEPARATE, non-superuser role that may
# only run SELECT on an explicit table allowlist, inside read-only transactions
# under a statement timeout. It is NEVER the app's DATABASE_URL: the Compose app
# role is a PostgreSQL SUPERUSER, so reusing it would put diagnostics reads one bug
# away from the encrypted credential store. The app refuses to start a diagnostics
# read unless the server itself confirms the role is least-privilege, and refuses
# outright when this names the same role as DATABASE_URL.
# Provision it with: AI_DIAGNOSTICS_DB_PASSWORD='<long random secret>' npm run diagnostics:provision-role
# See docs/ai-diagnostics/deployment.md.
AI_DIAGNOSTICS_DATABASE_URL=         # e.g. postgresql://ai_diagnostics_ro:<password>@postgres:5432/tacbookings?connection_limit=3

# Bootstrap provisioning (optional, DR / clone — ADR-003)
CONFIG_BUNDLE_IMPORT_PATH=           # Path to a config-transfer bundle auto-imported on boot ONLY when the DB is empty of non-seed configuration; leave empty to disable
