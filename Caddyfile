tokoroa.org.nz, www.tokoroa.org.nz {
	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		X-XSS-Protection "1; mode=block"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "camera=(), microphone=(), geolocation=()"
		Strict-Transport-Security "max-age=63072000; includeSubDomains"
		# CSP is emitted by Next proxy so script nonces are generated per request.
		# style-src intentionally keeps 'unsafe-inline' there while Tailwind/Radix runtime styles are audited.
	}
	handle /finance-legacy* {
		forward_auth app:3000 {
			uri /api/finance/legacy-dashboard/auth
		}
		reverse_proxy 172.26.3.200:8501
	}
	import /etc/caddy/deploy/tacbookings-active.caddy
}

bookings.tokoroa.org.nz {
	@root path /
	redir @root https://tokoroa.org.nz/dashboard permanent
	redir * https://tokoroa.org.nz{uri} permanent
}

dashboard.tokoroa.org.nz {
	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		X-XSS-Protection "1; mode=block"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "camera=(), microphone=(), geolocation=()"
		Strict-Transport-Security "max-age=63072000; includeSubDomains"
	}
	redir * https://tokoroa.org.nz/finance-legacy{uri} permanent
}

xero-mcp.tokoroa.org.nz {
	reverse_proxy xero-mcp-auth:8080
}
