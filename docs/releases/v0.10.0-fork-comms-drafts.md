# v0.10.0 fork-communication drafts — DRAFT, NOT POSTED

Status: **drafts for the owner to review and post.** Nothing here has been sent.
Posting these comments on downstream forks is owner-gated and deferred to
post-approval. Fill in the exact issue/PR links on the target fork before
posting.

Context: the remediation epic #1348 close-out (issue #1379, findings F30/F32)
determined that two downstream forks need a direct heads-up that fixes they are
waiting on have landed upstream and require pulling past a specific PR.

---

## (i) triyder's fork — Booking Officer lockout fix needs pulling past PR #1326

**Where to post:** the open issue on triyder's fork tracking the Booking Officer
lockout (the one reporting that non-Full-Admin Booking Officers cannot see
booking detail). Adjust the greeting to match.

> Hi — the Booking Officer lockout you reported is fixed upstream, but the fix
> spans two PRs, so pulling only the first one won't fully resolve it.
>
> - **PR #1325** (issue #1289) changed the member-detail / booking-detail viewer
>   gate from Full-Admin-only to area-level admin access
>   (`hasAdminAreaAccess` / `canViewAsAdmin`), so a Booking Officer regains the
>   booking views their seeded permission matrix grants.
> - **PR #1326** (issue #1303) then separated the admin and member payment
>   controls on the booking-detail surface, which is the part that makes the
>   Booking Officer view behave correctly rather than exposing member-only
>   payment actions.
>
> To get the complete fix, **sync your fork past PR #1326** (it is included in
> the upstream `v0.10.0` release). Pulling only up to #1325 leaves the payment
> controls half-migrated. The wider on-behalf write-capability gap that came out
> of this was tracked separately as #1313 and closed by PRs #1343/#1345
> (also in `v0.10.0`), so if you also want on-behalf create/quote for
> `bookings:edit` holders, that ships in the same release.
>
> Note: the earlier "Booking Office" → "Booking Officer" label change (PR #1168)
> was display-only — the seeds are key-based, so the label never affected
> access. The lockout was the Full-Admin-only route gate, now fixed as above.
>
> Recommended path: upgrade tag-to-tag to `v0.10.0` and follow
> `docs/UPGRADING.md`.

---

## (ii) hoppers99 issue #5 — bed-allocation contention semantics

**Where to post:** hoppers99 issue #5. Also link the new upstream tracking issue
once it is filed (see the note below).

> Hi — following up on the bed-allocation contention behaviour from this thread.
>
> The provisional-bed board behaviour you raised was addressed upstream by
> **PR #1261** (issue #1251), which made the bed-allocation board show
> provisional holds distinctly, and by **PR #1410** (issue #1387), which added
> capacity-holding priority so a booking that actually needs a bed deprioritises
> provisional occupants during auto-allocation. Both are in the upstream
> `v0.10.0` release.
>
> There is a remaining semantics question we are now **tracking upstream**: the
> auto-allocator treats a provisional bed as occupied, while availability counts
> that night as free — so it is not yet fully settled whether first-fit should
> deprioritise or evict a provisional bed when a capacity-holding booking needs
> it. We have opened an upstream issue for that specific contention rule and
> linked it here: **<UPSTREAM_ISSUE_LINK>**. Please follow that issue for the
> decision and any follow-up change.
>
> If you upgrade to `v0.10.0` you already get the provisional-hold visibility and
> the capacity-holding priority; the open item above is a further refinement, not
> a regression.

---

## Owner action checklist (post-approval)

- [ ] File the upstream bed-allocation contention issue (auto-allocation treats
      provisional beds as occupied while availability counts the night free;
      should first-fit deprioritise/evict provisional beds when a
      capacity-holding booking needs one?) and paste its link into draft (ii).
- [ ] Confirm the exact target issue on triyder's fork for draft (i).
- [ ] Post draft (i) on triyder's fork.
- [ ] Post draft (ii) on hoppers99 issue #5.
